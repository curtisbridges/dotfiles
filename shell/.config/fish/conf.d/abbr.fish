#
# Fish abbreviations (other shells use alias)
#

abbr -a bup 'brew update; and brew upgrade; and brew upgrade --cask --greedy'

# Simple executable abbreviations
abbr -a nf 'neofetch'
abbr -a cat 'bat'
abbr -a top 'htop'
abbr -a vim 'nvim'
abbr -a grep 'ripgrep'

# tmux
abbr -a tm 'tmux'
abbr -a tmas 'tmux attach-session -t'
abbr -a tmls 'tmux list-sessions'
abbr -a tmks 'tmux kill-session -t'
abbr -a tmns 'tmux new-session -t'

